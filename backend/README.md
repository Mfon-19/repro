# Repro FastAPI Backend

## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
make dev
```

The API will run on `http://localhost:8080` by default.

## Environment

See `.env.example` for the full list of variables.
